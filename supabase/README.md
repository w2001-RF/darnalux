# Supabase
Add migrations and Edge Functions here. Production secrets must never be committed. The web app uses only the public Supabase URL and anon key; privileged integration secrets belong in Edge Functions.
